{{- if and (eq (default "hub" .Values.mode) "hub") .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.serviceName }}
  namespace: {{ include "rhacs-manager.namespace" . }}
  labels:
    {{- include "rhacs-manager.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.frontend.service.annotations | nindent 4 }}
spec:
  selector:
    app: {{ .Values.frontend.deploymentName }}
  ports:
    - name: {{ .Values.frontend.service.portName }}
      port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.targetPort }}
    {{- if and .Values.mcp.enabled .Values.mcp.plainHttp.enabled }}
    - name: mcp-http
      port: {{ .Values.mcp.plainHttp.port }}
      targetPort: {{ .Values.mcp.plainHttp.port }}
    {{- end }}
{{- end }}
